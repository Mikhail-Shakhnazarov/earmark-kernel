# v0.3 format: standing dimensions use kernel:* prefixed IDs
name: source_note
version: 0.2.0
kind: object
required_headers:
  - title
payload_schema: inline:any
standing_rules:
  allowed_standing:
    kernel:epistemic:
      - working
    kernel:review:
      - unreviewed
    kernel:process:
      - completed
relation_rules: []
validators: []

