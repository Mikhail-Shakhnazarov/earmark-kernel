# v0.3 format: standing dimension constraints use kernel:* prefixed IDs
name: quellnotiz
version: 0.2.0
kind: object
required_headers:
  - title
payload_schema: inline:any
standing_rules:
  allowed_standing:
    kernel:epistemic:
      - working
      - supported
    kernel:review:
      - unreviewed
      - accepted
    kernel:process:
      - active
      - completed
relation_rules:
  - relation_type: source_of
    counterparty_classes:
      - befund
validators: []
