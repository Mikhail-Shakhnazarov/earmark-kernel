name: closure
version: 0.1.0
kind: object
required_headers:
  - title
payload_schema: inline:any
standing_rules:
  allowed_standing:
    kernel:process:
      - completed
      - deferred
      - rejected
      - superseded
relation_rules: []
validators: []
